import { StyleSheet } from "react-native";


export default StyleSheet.create({
    errorMessage: {
        color: "tomato",
        fontWeight: "500",
        display: "flex",
        position: "absolute",
        top: 40
    }

})
